Pwd - Present working directory
whoami - Get the active username
sudo su -  ---> switch to root user
chmod - Command to change file permissions
chown - Command for granting ownership of files or folders
mkdir - create folder
ls - list the content
ls -l Content
Cat - look the content
echo - prints the content
mv - move the content
cp - copy
rm - remove
194  ip addr show
195  free
free -m
   df
 df -h
  cat /etc/hosts
 findmnt
sudo useradd
Use----> Tab for auto completion
history
!f ----> goes to previous command
ctrl r for reverse searching
Piplining :taking First Output to use second command
Redirection:ls > lsfiles
cat lsfiles
 whoami > lsfiles
cat lsfiles
ls >> lsfiles ---> Append means add
Env Variables ---> env
 alias
grep -R linuxteam /etc
grep -R linuxteam /etc 2> /dev/null
cat /etc/os-release
 cd home
   10  ls -lrt
   11  useradd example
   12  ls
   13  ls -lrt
   14  cd /usr
   15  ls
   16  cd /var
   17  ls -lrt
   18  cd log
   19  ls
   20  less messages

man pages -- > user information
man  man
man ls
  253  man man
  254  man reboot
  255  man -k user
  256  man -k user | grep user
  257  man apropos
  258  man db
  259  mandb
